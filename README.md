
# RX∞8 Backend (Render)

- Start command: `python server.py`
- Env vars: `GEMINI_API_KEY` (optional), `MODEL_DEFAULT` (optional), `ALLOW_ORIGIN` (optional)
- Endpoints: `/health`, `/config`, Socket.IO at `/socket.io/` (event: `chat_message`)
