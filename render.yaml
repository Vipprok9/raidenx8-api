services:
- type: web
  name: rx8-backend
  env: python
  plan: free
  buildCommand: pip install -r requirements.txt
  startCommand: python server.py
  envVars:
  - key: OPENAI_API_KEY
    sync: false
  - key: GEMINI_API_KEY
    sync: false
